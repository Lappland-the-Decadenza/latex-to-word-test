[pytest]
# Keep pytest's disposable base directory outside the repository.  The Codex
# sandbox may own files it creates, so project-local test scratch can become
# undeletable by the interactive user after an interrupted run.
addopts = -p no:cacheprovider
